# 第 2 章：图形渲染管线

> 来源：《Real-Time Rendering, Fourth Edition》Chapter 2  
> 学习状态：已完成首次阅读；第二次复习已推进至 2.5 像素处理与合并

## 一句话概括

实时渲染管线把场景中的相机、几何、材质和灯光，持续转换为二维图像。CPU 决定这一帧提交什么，GPU 负责把三角形变换、光栅化、着色并写入显示缓冲。

```text
场景数据
  │
  ├─ 应用阶段（CPU）：更新场景、选择并提交待画图元
  └─ GPU：几何处理 → 光栅化 → 像素处理/合并 → Back Buffer → Present
```

## 2.1 架构

### 四个功能阶段

| 阶段 | 主要位置 | 回答的问题 | 典型工作 |
| --- | --- | --- | --- |
| Application | CPU（也可借助 GPU Compute） | 这一帧要画什么？ | 输入、游戏逻辑、动画、物理、剔除、提交绘制命令 |
| Geometry Processing | GPU | 三角形在相机看来在哪里？ | 顶点处理、投影、裁剪、屏幕映射 |
| Rasterization | GPU 固定功能硬件为主 | 三角形覆盖哪些采样点？ | 三角形设置、遍历、插值、生成片元 |
| Pixel Processing | GPU | 这些片元是什么颜色，能否写入画面？ | 像素着色、深度/模板测试、混合、写入缓冲 |

### 管线思维

- 管线提高的是**吞吐量**：当前一帧的后段在工作时，后续工作可在前段开始准备。
- 最慢阶段是**瓶颈**，决定整帧的最高速度。
- 60 FPS 的预算约为 `16.67 ms`，120 FPS 约为 `8.33 ms`。
- 书中的四段式是功能模型；实际硬件会并行、重叠或拆分这些功能。

## 2.2 应用阶段

### 职责

应用阶段通常在 CPU 上运行，负责在每一帧中：

1. 读取输入并更新游戏/应用逻辑。
2. 更新相机、动画、物理、碰撞和场景物体的变换。
3. 执行可见性判断，例如视锥剔除。
4. 整理可见物体所需的网格、材质、纹理和参数。
5. 提交 draw call 或间接绘制命令给 GPU。

### Draw Call

一次 draw call 可以理解为 CPU 对 GPU 的指令：使用当前 shader、资源和渲染状态，绘制某个网格或其一部分。

常见状态包括：

- Shader / Pipeline State；
- 顶点和索引缓冲；
- 纹理、常量缓冲、材质参数；
- 深度、混合、剔除等渲染状态。

### 性能结论

- 过多小物体或不同材质，常导致 CPU 被 draw call 和状态设置拖慢。
- 对相同网格、相同材质的多个实例，可用 instancing 降低调用次数。
- CPU 可做粗粒度剔除；现代 GPU-driven 渲染也会在 GPU 上做实例或 cluster 级剔除。

## 2.3 几何处理

### 顶点的空间旅程

```text
模型空间（Local / Model）
  → 世界空间（World）
  → 观察空间（View / Eye）
  → 裁剪空间（Clip）
  → NDC
  → 屏幕/窗口坐标（Screen / Window）
```

概念上可写为：

```text
clipPosition = Projection × View × Model × localPosition
```

矩阵库可能采用不同的向量约定，导致代码中的乘法顺序不同；空间变换的概念顺序不变。

### 顶点着色器

Vertex Shader 的职责不只是“计算顶点光照”，更准确地说是加工每个顶点的数据：

- 必须输出顶点位置；
- 还可输出 UV、法线、顶点色、切线、世界坐标等；
- 输出数据会在光栅化时插值，供像素着色器使用。

### 投影、裁剪、NDC

- 正交投影：远近不改变大小，平行线保持平行。
- 透视投影：远处物体更小，来自透视除法。
- 投影后先得到齐次裁剪坐标 `(x_clip, y_clip, z_clip, w_clip)`。
- 裁剪在 Clip Space 中完成，完全在外的图元丢弃，部分相交的图元被切出可见部分。
- 然后执行透视除法：`(x/w, y/w, z/w)`，得到 NDC。
- NDC 再经 viewport 变换，映射到具体分辨率的窗口坐标。

### 可选顶点处理阶段

- Tessellation：根据距离等条件增加或减少曲面三角形密度。
- Geometry Shader：按图元生成有限的新顶点，曾常用于粒子等用途。
- Stream Output：将 GPU 处理后的几何输出到缓冲区供后续使用。

这些不是理解基础光栅管线的前提；现代引擎也经常以 Compute、Mesh Shader 或 GPU-driven 流程替代其中部分用途。

## 2.4 光栅化

光栅化把屏幕空间中的点、线、三角形转成一批片元（fragment）。它并不计算最终颜色。

```text
三角形顶点 + 每顶点数据
  → Triangle Setup：计算边与插值所需数据
  → Triangle Traversal：判断哪些采样点被覆盖
  → 片元：深度 + 插值后的 UV、法线、颜色等
```

- 最简单的覆盖规则是“像素中心在三角形内”。
- MSAA 使用每像素多个采样点，改善边缘锯齿。
- UV、法线和深度等属性会从三个顶点插值到每个片元。
- 透视正确插值会利用投影时保留的 `w`，避免倾斜表面上的纹理错误扭曲。

## 2.5 像素处理与合并

```text
片元
  → Pixel Shader：纹理采样、材质和光照计算
  → Merging / ROP：深度、模板、混合与写入缓冲
```

### Pixel Shader

- 接收插值后的 UV、法线、位置等数据。
- 采样纹理并计算材质、光照，输出一个或多个颜色结果。
- 运行频率通常是每片元或每采样点一次；覆盖面积越大、分辨率越高、shader 越复杂，代价越高。

### 深度、模板和混合

- **Depth Buffer（Z Buffer）**：保存当前最近表面的深度；不透明物体通常开启深度测试和深度写入。
- **Stencil Buffer**：每像素标记，可制作掩码、传送门、描边等效果。
- **Alpha Blending**：将新片元颜色与已存颜色混合；透明物体通常在不透明物体之后，从远到近渲染，并关闭深度写入。

### 性能：Overdraw

如果一个屏幕位置的片元多次被完整着色，但前面的结果随后又被更近物体覆盖，就发生了过度绘制。Early-Z 会尽量在执行昂贵像素着色器前剔除已知不可见的片元；使用 `discard`、修改深度或某些透明流程会限制它的效果。

### 显示

- GPU 通常向 Back Buffer 渲染，而不是直接修改显示器上的当前画面。
- 完成后通过 Present / Swap 显示这一帧；现代 API 通常用 Swap Chain 管理这些缓冲。

## 2.6 一帧的完整路径

```text
输入 / 时间
  → CPU 更新相机、动画、物体矩阵与可见性
  → 提交可见图元与绘制命令
  → GPU 变换顶点、投影和裁剪
  → 光栅化生成片元并插值属性
  → 像素着色器采样纹理、计算材质和光照
  → 深度/模板/混合决定最终缓冲内容
  → Present 到屏幕
```

关于“纹理应用”的精确分工：光栅化负责插值 UV；纹理采样通常发生在像素着色器中。

## 易混淆概念

| 概念 | 含义 |
| --- | --- |
| 剔除（Culling） | 整个图元/物体不需要画，直接跳过。 |
| 裁剪（Clipping） | 图元部分可见，沿视锥边界切出可见部分。 |
| 像素（Pixel） | 屏幕上的最终离散位置。 |
| 片元（Fragment） | 光栅化后等待测试/合并的候选结果，未必真的成为最终像素。 |
| Clip Space | 透视除法之前、用于裁剪的齐次坐标空间。 |
| NDC | 透视除法之后的归一化坐标空间，再映射到实际屏幕。 |

## 问答记录

### Q1：为什么大量不同材质会导致状态切换和 draw call？为什么影响性能？

**答：** 一个材质不仅是一张贴图，还通常包含 shader、纹理、常量参数以及深度、混合、剔除等状态。绘制使用不同材质的物体时，CPU 往往要重新设置或绑定这些状态并发出新的 draw call。

- CPU/驱动需要准备、验证和记录更多命令；调用数很高时，GPU 可能等待 CPU 继续提交工作。
- GPU 更适合连续执行相同的 shader 和资源绑定；频繁切换会降低批处理机会，并可能降低缓存利用率。
- 解决思路包括按材质排序、合批和 instancing。透明物体通常需由远到近绘制，不能随意按材质排序。

### Q2：NDC 是什么？

**答：** NDC（Normalized Device Coordinates，归一化设备坐标）是顶点完成投影、裁剪和透视除法后所在的标准化坐标空间。x、y 通常落在 `[-1, 1]` 内；z 的约定因 API 而异，DirectX/Vulkan 常用 `[0, 1]`，传统 OpenGL 常用 `[-1, 1]`。

```text
Clip Position：(x_clip, y_clip, z_clip, w_clip)
NDC：(x_clip / w_clip, y_clip / w_clip, z_clip / w_clip)
```

NDC 与实际窗口分辨率无关；随后 viewport 变换才会把它映射到如 1920×1080 的像素坐标。

### Q3：面对超大地图，应用阶段的 CPU 主要处理什么？

**答：** CPU 通常不逐顶点或逐像素处理整张地图，而是管理世界并逐步缩小本帧工作集：根据玩家位置流式加载附近区域，通过空间索引查询潜在可见对象，进行视锥、距离和可选遮挡剔除，选择 LOD，更新动画与变换，整理材质和实例批次，最后生成并提交绘制命令。现代 GPU-driven 管线会把实例或 cluster 级剔除、LOD 选择和间接命令生成的一部分移到 Compute Shader；CPU 仍负责高层场景管理、资源生命周期、任务调度与同步。

### Q4：CPU 是否会根据各种策略决定 draw call 的数量和方式？

**答：** 是。CPU 会根据可见性、LOD、材质、渲染 Pass、透明排序和批处理策略决定本帧画什么、如何分批以及如何提交。目标不是无条件减少 draw call，而是在 CPU 提交成本与 GPU 实际工作量之间取得平衡。传统流程由 CPU 直接生成命令；GPU-driven 流程会把细粒度剔除、LOD 选择和间接命令生成部分交给 GPU。

### Q5：Unity 或 Unreal 面对大地图时，CPU 如何准备 draw call？

**答：** 两类引擎通常先通过分区和流送决定哪些区域驻留内存，再把游戏世界的变化同步到稳定的渲染场景；随后针对主相机、阴影等不同 View 做空间查询、视锥/距离/遮挡剔除和 LOD 选择，为每个渲染 Pass 生成绘制项，再按 PSO、材质、深度等排序并进行合批或实例化，最终记录图形 API 命令。Unity 常见 SRP Batcher、GPU Instancing、BatchRendererGroup 等路径；Unreal 常见 Scene Proxy、Mesh Draw Commands、HLOD、GPU Scene、Instance Culling 和 Nanite 等机制。

### Q6：Vertex Shader 在哪个阶段运行？输入和输出是什么？

**答：** Vertex Shader 是几何处理阶段的可编程阶段，在 Input Assembler 之后、光栅化之前逐顶点运行。输入包括顶点缓冲中的位置、法线、切线、UV、颜色，以及矩阵、骨骼、材质和实例参数等共享数据；必须输出裁剪空间位置，还可输出 UV、世界法线、世界位置等，供光栅化器插值后传给 Pixel Shader。

### Q7：为什么 Vertex Shader 输出裁剪空间位置？此时 MVP 是否完成？

**答：** 基础管线中 MVP 已完成，输出为 `Projection × View × Model × localPosition` 得到的四维齐次裁剪坐标 `(x, y, z, w)`，但尚未透视除法。固定功能管线先利用 `w` 在 Clip Space 裁剪，再除以 `w` 得到 NDC，最后执行 viewport 变换。有 Tessellation 或 Geometry Shader 时，由光栅化前最后一个可编程几何阶段提供裁剪空间位置。

### Q8：Vertex Shader 效果通常做什么？为什么在这一阶段做？

**答：** 常见用途包括 MVP 变换、骨骼蒙皮、风吹草木、水面和地形位移、Morph Target、Instancing 与 Billboard，并准备需要插值给 Pixel Shader 的属性。凡是会改变几何位置和轮廓的工作，都必须在光栅化确定覆盖范围之前完成。它适合大量相互独立的逐顶点计算，但只能移动已有顶点，网格过稀时无法凭空产生细节。

### Q9：没有 Early-Z 时，隐藏片元是否一定会造成 overdraw？

**答：** 背面剔除会在片元生成前丢弃整个背面三角形；其余被遮挡表面仍可能经过光栅化。若完全没有 Early-Z，隐藏片元通常先执行 Pixel Shader，再被 Late-Z 拒绝，因此会产生无效的像素着色。Early-Z 主要节省 Pixel Shader 成本，并不消除顶点处理、三角形设置、覆盖判断和深度比较等全部成本。

### Q10：背面剔除在哪个阶段执行？

**答：** 背面剔除属于光栅化器的固定功能处理，发生在几何处理完成、三角形已投影到屏幕之后，以及覆盖遍历和片元生成之前。它根据屏幕空间顶点绕序判断正背面，可以一次丢弃整个三角形。

## 第二次复习记录

### 本轮已建立的主线

- 大地图的 CPU 应用阶段通过流送、空间查询、剔除、LOD、排序和合批，把庞大世界缩小为本帧可提交的工作集。
- Vertex Shader 逐顶点执行，完成几何变形和裁剪空间位置输出；裁剪、透视除法与 viewport 变换由后续固定功能阶段完成。
- 光栅化器进行背面剔除、覆盖判断和属性插值，生成片元；片元尚不是最终像素。
- Pixel Shader 计算候选颜色，合并阶段再通过深度、模板和混合规则决定 framebuffer 的最终变化。

### 已提出、下次需要重点深入的问题

1. Top-left rule 如何确定相邻三角形公共边的归属。
2. UV 为什么不能直接在屏幕空间线性插值；透视正确插值的公式与推导。
3. Early-Z 的执行位置、层次化深度、适用条件以及受 `discard`、写深度等操作影响的情况。
4. 一个像素/采样点处的属性如何插值；重心坐标如何计算。

### 下次建议的复习顺序

```text
重心坐标与普通属性插值
  → 透视投影为何破坏屏幕空间线性关系
  → 透视正确 UV 插值
  → Top-left 覆盖规则
  → Early-Z、Late-Z 与 Overdraw
```

## 复习清单

- [ ] 能说出四个功能阶段及其主要职责。
- [ ] 能从 Local Space 写到 Screen Space 的顶点空间路径。
- [ ] 知道裁剪在透视除法前的 Clip Space 发生。
- [ ] 能区分 rasterization、fragment 与 pixel shading。
- [ ] 能解释 Depth Buffer 为什么能解决不透明物体遮挡。
- [ ] 能解释透明物体为什么通常需要从远到近画。
- [ ] 能解释 draw call、状态切换与批处理的关系。
- [ ] 能说明大地图从世界分区到绘制命令的 CPU 准备过程。
- [ ] 能说明 Vertex Shader 的输入、输出以及为何输出 Clip Position。
- [ ] 能区分背面剔除、光栅化、Early-Z 和 Late-Z 的执行粒度与顺序。
- [ ] 能推导重心坐标及透视正确插值。
